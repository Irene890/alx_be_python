# length represents the length of the rectangle
# width represents the width of the rectangle
# Script calculats the area of the rectangle using the formula (Area = length × width) and store the result in a variable named area

length = 10
width = 5

area = length * width

print("The area of the rectangle is:", area)